fix: polish for local boot — migrations, Docker entrypoint, auth redirect, env

- Generate drizzle migration files (drizzle/0000 + 0001)
- Add docker-entrypoint.sh that runs migrations before starting the server
- Add scripts/migrate.js standalone migration runner (no drizzle-kit needed at runtime)
- Update Dockerfile to copy migration files and use entrypoint script
- Fix requireAdmin() to redirect to /admin/login instead of throwing
- Fix .env.example to match actual auth implementation (ADMIN_PASSWORD not ADMIN_PASSWORD_HASH)
- Add .env.local for local development
- Add db:index.ts re-export for single connection pool
- Clean up unused imports and variables
- Reconcile schema with lead agent's projects feature

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
